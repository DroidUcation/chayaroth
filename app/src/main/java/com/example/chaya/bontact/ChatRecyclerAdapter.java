/*package com.example.chaya.bontactapp;

import android.support.v7.util.SortedList;
import android.support.v7.widget.RecyclerView;
import android.support.v7.widget.util.SortedListAdapterCallback;
import android.view.View;
import android.view.ViewGroup;
import android.widget.TextView;

/**
 * Created by chaya on 5/30/2016.
 */
/*
public class ChatRecyclerAdapter extends RecyclerView.Adapter{

    SortedList<Long> data = new SortedList<Long>(Long.class, new SortedListAdapterCallback<Long>(this) {
        @Override
        public int compare(Long o1, Long o2) {
            return o1.compareTo(o2);
        }

        @Override
        public boolean areContentsTheSame(Long oldItem, Long newItem) {
            return false;
        }

        @Override
        public boolean areItemsTheSame(Long item1, Long item2) {
            return false;
        }
    });

    @Override
    public RecyclerView.ViewHolder onCreateViewHolder(ViewGroup parent, int viewType) {
        return new VH(new TextView(parent.getContext()));
    }

    @Override
    public void onBindViewHolder(RecyclerView.ViewHolder holder, int position) {

    }

    @Override
    public int getItemCount() {
        return 0;
    }

    public void addItem(Long g) {
        data.add(g);
    }

    public static class VH extends RecyclerView.ViewHolder {

        public VH(View itemView) {
            super(itemView);
        }
    }
}*/
